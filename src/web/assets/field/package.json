{
  "name": "@lindemannrock/icon-manager-field",
  "version": "1.0.0",
  "private": true,
  "description": "Build tools for Icon Manager field assets",
  "scripts": {
    "build": "npm run build:js",
    "build:js": "mkdir -p dist && terser src/IconManagerField.js -o dist/IconManagerField.js -c -m"
  },
  "devDependencies": {
    "terser": "^5.46.1"
  }
}
